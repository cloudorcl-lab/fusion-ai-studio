# P2 supplier transaction contract (preparation; implementation pending)

Source: plan required-only scope; 26C supplier reference, current eqih-dev21 identity and native Dell Inc. scalar GET. Current GET confirms BusinessRelationship=Spend Authorized and TaxOrganizationType=Corporation. These reference values must be explicitly supplied/displayed in the native draft, not silently defaulted. Distinctive fixture name is generated for this build and field-edited before review.

Payload exactly Supplier, BusinessRelationship, TaxOrganizationType. Supplier has collection-wide exact-name duplicate preflight (case/whitespace normalization remains unverified). Generated SupplierId, SupplierNumber, SupplierPartyId omitted. All optional fields and child collections omitted per approved plan. Upsert disabled. Independent item GET verifies all three intended business values and generated IDs. Preserve successful write response and never replay it.

Use separate CONVERSATION transactionState, with readState untouched. Deterministic code owns requestId, normalized payload, revision, review, approval and attempt state. Models extract only allowed patches. Exact full-message controls govern review/approve/create/cancel. Review must be current before approval; approval stores exact payload, requestId and revision; every material edit clears review/approval. A create command must name the same revision and match the stored approval exactly. Persist a pending attempt before POST; duplicate/error/incomplete preflight submits nothing. A pending/unknown outcome permits read-only reconciliation, never an automatic POST retry. Accepted result persists returned ID and GET comparison. Conversation state is not a cross-session atomic idempotency store; the app must not claim exactly-once behavior across independent concurrent conversations.

Native acceptance sequence: prepare required fields with source shown; field-only name edit; review complete payload; exact revision approval; exact revision create; one POST; independent persisted GET; complete native result. Prove cancelled/stale approval locally and through safe native turns without additional accepted creates. One accepted supplier transaction is the gate before any child implementation.
