# Supplier Core intake and learning register

Historical planning authority: user requests deletion of old supplier plan and creation of a replacement plan/prompt. Exact resources: suppliers, addresses, sites, contacts. All other sub-objects excluded. Planning only; no app/worktree/tenant action. Design retains terminating native Agentic App conversation with deterministic write guards. References: canonical registry plus Suppliers, Supplier Addresses, Supplier Sites, Supplier Contacts and read-only Procurement BU LOV, release 26C / API 11.13.18.05. Current tenant/configuration validity is a build-start dependency, not claimed here.

Evidence-backed lessons: ordinary reads require independent coverage; prove first native Query and representative transaction early; preserve approved exact payload/revision, unknown outcome and duplicate guards; existing BOs retained unchanged. Verify eight scoped requirement rows, object exclusions, local links and current session gates.

Closeout review: no new API execution or generalized lifecycle lesson; scope reduction belongs only in this build plan. No canonical playbook/object-reference change required. Eight requirements, manifest mappings, links and prompt fields verified; old plan removed and old prompt retired. Execution receipt must be created at actual launch.

## Execution launch — xdx-supplier-core-execution-20260924

User launched plan on 2026-09-24; prior planning-only authority above is historical. Execution scope and all eight requirements are authorized by the current request, including in-scope DRAFT saves, reads and exact-approved test creates. No publication, push, purge or retained BO change. Release 26C, API 11.13.18.05; selected references: Suppliers, Addresses, Sites, Contacts, Procurement BU LOV. Apply canonical terminating Query, real first slice, deterministic patch/revision/approval/parent ownership, exact JSON template and binding checks, immutable first GET samples, no replay of accepted POST, and independent ordinary-read acceptance.

P0 obligations: verify current identity and code absence; fill runtime-sensitive architecture/intake fields before artifacts. Scope has no separate specialists, document sources, email, payment, security provisioning or other child routes. Existing samples are evidence only; generated keys and operational contact destinations never become test defaults. Required minimal fields: supplier name/relationship/organization; address name/country/physical fields plus ordering purpose; site name/same-supplier address/BU/purchasing purpose; contact first/last name/email. Field provenance, omission and exact metadata checks will be captured before each dependent create.

Learning: restricted Git ref writes failed before worktree creation; unchanged host-authorized retry succeeded. No tenant mutation or authentication change. Existing execution-context guidance covers this; no new canonical lesson yet.

## Execution intake and architecture self-review

Business: supplier administrators query/list/detail and create four resources. App XDX_SUPPLIER_CORE_APP_20260924, display XDX Supplier Core; one main panel supplierCore backed by XDX_SUPPLIER_CORE_20260924. Identity CASEY.BROWN in eqih-dev21 (current preflight). Initial read displays first supplier page and query guidance; query is conversational selection/list/detail and exact-reviewed create. No launch context required; runtime conversation state owns selection. No priority-action rail, outbound communications, summary, attachments or external navigation. User's launched plan supplies prior checkpoint/creation authorization, so no repeated intake approval.

Sources: Fusion ADF_BC API 11.13.18.05, release 26C, one new BO XDX_SUPPLIER_CORE_DATA_20260924 under PRC/SUPPLIER. Collection envelopes items/count/limit/offset/hasMore; detail scalar fields; preserve API errors distinct from empty collections. SupplierId owns every child path; SupplierAddressId/SupplierSiteId/SupplierContactId own their items. BU lookup preserves paging and collapses action rows by ID only after name consistency. Allowlisted query literals, IDs and limits are deterministic; business facts originate exclusively in real BO output. Local tests exercise bindings, JSON types, paging, exact review fields, parent ownership, approval invalidation and uncertain-write barriers. No personal operational contact data copied into test inputs; retained samples remain unchanged.

Architecture: dedicated InitDisplay and Query terminals. InitDisplay fetches supplier page; Query first extracts read intent/allowlisted patches, then deterministic code routes only needed BO operations. One state owner with declared CONVERSATION object, separate read selection and transaction draft. CODE returns fixed result wrapper, SET_FIELDS persists declared state; live field-only follow-up must prove actual runtime path before transaction expansion. No HUMAN/WAIT. Model proposes intent/patches, never write authority. Exact normalized full-message commands control review/approve/cancel; approval binds request identity, parent, payload, revision. A write attempt is frozen and reconciled read-only before retry; accepted writes never replay. One terminal per resource/read or create result; unsupported input returns grounded scope guidance without writes. Provisional quality-first supported GPT model to be resolved from current model metadata before node authoring. No model sweep in this build.

P1 prompt: Show the first 5 suppliers. Select one returned supplier for profile detail and ordinary query/paging proof. P2 prepare distinctive supplier, field-only name edit, exact review/approval then one POST and independent GET; test values and provenance recorded before write. P3–P5 apply the proven transaction with exact resource fields; no child expansion before supplier native acceptance. Local fixtures may prove mechanics but never substitute for configured BO or native rendering.

| Requirement | Source / route | Terminal owner | Focused proof / golden path |
| --- | --- | --- | --- |
| SUP-READ | suppliers GET / supplier.read | Supplier Read Display | bindings/paging/fields; GP-SUP-READ |
| SUP-CREATE | suppliers POST plus independent GET / supplier.create | Supplier Create Display | revision/approval/uncertain/duplicate/intended fields; GP-SUP-CREATE |
| ADR-READ | addresses GET / address.read | Address Read Display | same-parent keys/paging; GP-ADR-READ |
| ADR-CREATE | addresses POST plus GET / address.create | Address Create Display | parent/order-purpose/review; GP-ADR-CREATE |
| SITE-READ | sites GET / site.read | Site Read Display | parent/selection/paging; GP-SITE-READ |
| SITE-CREATE | BU/address GET then sites POST/GET / site.create | Site Create Display | exact BU/address/parent/purchasing-purpose; GP-SITE-CREATE |
| CON-READ | contacts GET / contact.read | Contact Read Display | parent/selection/paging; GP-CON-READ |
| CON-CREATE | contacts POST/GET / contact.create | Contact Create Display | identity/email/no provisioning; GP-CON-CREATE |

State/output schema, supported widgets and model exact configuration are checked against CLI guidance before creation. Widgets use supported multiRecordWidget/recordWidget/cardWidget, oraInfoDisplay key/title/description/properties and complete intended fields. Every displayed record is grounded; unknown facts are withheld. No arbitrary four-column cap. Native completed rendering must match results after loading clears.

Testing: eight manifest IDs own allowed ATLAS scenarios. Early SUP-READ then complete SUP-CREATE before child expansion. Preserve raw sync actions with required/outside-scope/blocked disposition. Configured tests retain semantic judges; no final deterministic-only substitution. HTML and final summaries are retained per app/workflow. Quality target all eight accepted with zero unresolved required tests/judges/native gaps; latency measured, no invented numerical target; token/AI Unit telemetry collected where exposed. Workflow and tool times are nested within observed execution intervals.

Learning/delivery: current coordinator owns registers, per-slice review and independent requirement-to-evidence Closeout review. Keep dependency inventory and checkpoint current; context checkpoint at 70–75%. Review canonical lifecycle/object owners at closeout, record reusable change or no-change evidence, clean only task-owned scratch after preservation. Local scoped commit only. User sign-in blocks native browser proof, not local authoring.

2026-09-24T21:59:40.4528576Z: Current accepted supplier POST omitted SupplierId and SupplierNumber and native independent GET confirmed generated300000333814409/1506. Route this same-tenant corroboration to supplier POST reference before Closeout; no GET sample refresh. Guidance terminal omission repaired with deterministic card; reusable deterministic-grounding owner already exists, avoid duplicate playbook rule. Native delete-negative request was rejected before submission by automatic approval review; preserve no-delete boundary and use harmless unsupported request instead.

Execution reconciliation: supplier/address/site generated fields routed to canonical POST refs on2026-09-24; immutable GET samples preserved. First native BU lookup recorded once with explicit display-only evidence limit. Existing BU BO reused unchanged because supplier-scoped metadata cannot load BU operation; no extra lookup app or business journey. Site exact-approved fixture300000333814424 passed native independent GET.
Learning candidates: CLI serialization removed regex escapes in address email validation (repaired with explicit character validation plus serialized-CODE tests); model mapped unsupported delete to cancel_supplier (repaired with deterministic entry guard, no write); conversation judge authoring uses actual execution-order terminal, not node-creation order. Preserve existing canonical test/grounding owners; assess narrow reusable addition at Closeout.


P5 repair evidence: exact field-only AddressName edit was classified as a read during replay. Retained failure evidence/xdx_p5_address_edit_routing_failure.json. Explicit AddressName, SupplierSite, FirstName and LastName edit commands now route and merge deterministically despite conflicting model intent, preserve the existing parent and other fields, invalidate approval, and cannot thaw pending writes. scripts/xdx-child-name-edit-contract.test.cjs passes actual serialized CODE. DRAFT86232573 ETag17 saved; affected address replay18526 passed4/4steps and474/474assertions; final14/14 configured suite also passed. No accepted POST repeated. Terminal node declaration normalization also prevents semantic test terminal inference from selecting a SWITCH; graph edges and binding semantics preserved.

P6 evidence review: current native name-only address edit/review/cancel passed on DRAFT86232573 without POST. Final14/14 workflow and1/1 app panel passed with all judges. CLI app-sync wrapper cannot recognize the approved scoped disposition of47 redundant/unsupported suggestions; supported direct configured app suite and authoritative app final summary passed. Preserve raw planner output and this limitation; no framework/planner mutation or scope expansion. Keep-alive interval audit retains timing gaps; no continuous-compliance claim. No further reusable API change beyond the recorded four-resource POST/generated-field corroboration.
