# Build Intake and Learning Register

Task: xdx-supplier-lifecycle-plan-20260920. Scope: phased plan, executable build/test prompt, server BO reuse discovery, isolated new worktree bootstrap. No application implementation or live supplier creation in this task.

Authority: current user request. Ask Oracle, agent lifecycle, four-column displays, source-based approved inheritance, exact draft approval, generated ID classification before each write slice. Detailed field research is deferred to execution as requested.

Selected references: Procurement suppliers, supplier-addresses, supplier-sites, supplier-contacts; Oracle26C/API11.13.18.05. Additional children require inventory and individual contracts in Phase0.

Lessons: prove actual conversational continuity first; keep state and approval at root; independent persisted GET is required but does not substitute for browser acceptance. Canonical playbook and object registry remain owners. No new evidence-backed lesson to promote during planning.

Environment: active root env.properties only. Reuse server XDX supplier BO definitions before creating dependencies; discover/read only now. No GitHub push or application publication. Worktree owner: current Codex task under explicit user instruction; cleanup after accepted integration or explicit abandonment, preserving evidence first.

## Planning findings
- Supported catalog found both XDX supplier BOs; 12 supplier functions and one BU lookup. Recommendation endpoint HTTP404 is bypassed through documented catalog, not hidden APIs.
- Sandboxed host credential access failed; supported unsandboxed retry authenticated successfully. No credentials recorded.
- BO metadata/fetch scope only; detailed current field research and supplier data operations deferred.
- Source snapshots retained for version/compatibility comparison; no runtime artifact adopted or modified.
- Existing source reference has eight GET functions on server, including validated filter, not just historical seven. Server evidence owns the reuse inventory.

## Execution intake
The plan owns outcomes, routes, state, phases, source scope and measurable correctness. P0 must fill authenticated execution identity, exact Ask Oracle binding, source field contracts/children, model placement, approved test identities and live-write counts before dependent artifacts or operations. These are execution gates, not completed planning evidence.

## Worktree bootstrap reconciliation — 2026-09-22

Session/task: `01a0c9e8-b085-7720-8d22-94df575b2834`. Authority: current explicit request to create the exact worktree, preserve one active goal without a token budget and execute the approved plan under canonical gates. This session began in the base; it can prepare governance evidence but cannot replace the required fresh worktree run.

Observed correction: the prior handoff claimed an existing worktree, but `git worktree list --porcelain` listed only the base and the target path did not exist. Committed handoff `44c0d23` is a direct child of base HEAD `b1e9b4a` and changes only the handoff. Created the requested branch/worktree from that commit, leaving the base's modified handoff, skill-audit records and temp files untouched. Worktree owner is this task; cleanup only after accepted integration or explicit abandonment with evidence preservation.

Selected reviewed references remain suppliers, addresses, sites and contacts (Oracle 26C/API 11.13.18.05). P0 must still inventory every additional/nested child and create capability before architecture reliance. The first-success samples remain immutable. No current tenant/schema claim is inferred from historical examples.

All eight receipt groups remain planned for the full delivery goal. Startup preparation does not pass P0-P9. Runtime/app-stage/ATLAS/model/judge tests are pending, not excluded. Preserve exact-payload approval, version-safe BO reuse, intended-field/persisted-field comparison and independent UI acceptance. No new reusable lifecycle or resource lesson is established by bootstrap; the existing fresh-run and checkout-verification rules already cover this correction. Timing reconstruction and gaps are in time-tracker.md.

## Restart-policy correction

Task 01a0c9e8-b085-7720-8d22-94df575b2834-restart-policy: explicit user authority removes mandatory fresh sessions after worktree creation, governance changes and context checkpoints. Retain handoff-first review, current checkout identity, reread changed governance, new or reconciled task receipts, timing, ATLAS and Closeout. Scope: canonical policy, entrypoints, startup delivery snapshot/templates, verifier regression and current supplier execution guidance in both the target worktree and base. Historical install archives and receipts are retained as historical evidence. No business-object contract, app, runtime or live data changes. Existing plan design remains approved.

Policy correction evidence review: canonical lifecycle owner refined, duplicate startup requirements reconciled, current handoff and checkpoint corrected. No operation-level lesson or object change. Regressions retain evidence, timing and incomplete-Closeout rejection. Historical ZIP/install records retained without claiming repackaging. Only task-owned branch files will be committed; base policy propagation remains uncommitted to preserve its prior handoff changes. Verification receipts: evidence/xdx_restart_policy_verification.json and evidence/xdx_restart_policy_parity.json.

## Execution task — xdx-supplier-lifecycle-execution-20260922

Scope and outcome: execute the approved P0-P9 plan for `XDX_SUPPLIER_LIFECYCLE_AGENT` in this worktree. Deliver actual Ask Oracle query, prepare, exact approval, guarded create and independent confirmation for suppliers and every P0-inventoried in-scope child. Preserve the four-column query limit, typed root conversation state, generated-ID rules and separate reference-default versus create approvals.

Authority: local authoring, authenticated discovery/reads needed by the approved plan, and version-safe remote BO/workflow DRAFT saves are authorized. No workflow CLI publication, GitHub push, destructive cleanup, account provisioning or live supplier/child POST is authorized. Before the first live POST, present the exact environment, counts, identities and payloads for scoped approval. Historical POST allowance does not transfer.

Current identity and layout: worktree `C:\Users\dasu\Documents\GitHub\fusion-ai-studio-1\.worktrees\xdx-supplier-lifecycle-agent`, branch `codex/xdx-supplier-lifecycle-agent`, Startup head `cded284d9ff79176c37d662994c17e4f308c9ef9`, legacy `src` layout because `app-pkg/` has no packages. Use only this worktree's `env.properties`; do not expose values.

Selected references: suppliers, supplier addresses, supplier sites and supplier contacts under Oracle Procurement 26C / API 11.13.18.05. P0 must enumerate every additional or nested supplier child and create capability from current authoritative specification, create object references before relying on them, and instantiate P8 requirement/test rows. Existing first-success BO GET samples remain frozen.

Evidence obligations: current environment and authenticated identity; exact BO versions and consumers; exhaustive resource/operation inventory; supported Ask Oracle entry contract; one owner/route/terminal/acceptance per requirement; focused ATLAS next actions; discrete and cumulative configured-mode evidence; actual UI confirmation; intended versus submitted versus persisted field reconciliation; timing/tokens/AI Units; dependency cleanup and full build Closeout. Planning/local checks cannot satisfy runtime acceptance.

## P0 execution findings — 2026-09-22

Authenticated identity is CASEY.BROWN in the configured eqih-dev21 environment. The first sandboxed credential-store call failed as expected and the supported unsandboxed retry succeeded; credentials and configuration values are not recorded. Current catalog discovery returned `hasMore=false`.

`XDX_SUPPLIER_INFORMATION` is now ETag 8 with 32 functions (24 GET, 8 POST), not the planning snapshot's ETag 2/12 functions. Twenty read/attachment functions were added after planning. `XDX_SUPPLIER_PROCUREMENT_BUSINESS_UNITS` remains ETag 1 with one GET. Read-only fetches were retained under `server-current/`; both validate with zero errors. No active local workflow consumes either BO. The exact current definitions were adopted under `src/businessObjects/`, preserving server identity and avoiding a stale remote overwrite.

Oracle 26C exposes 22 child resource rows in the selected supplier tree: three core resources and 19 additional/nested resources. The exhaustive operation/disposition map is in `resource-inventory.md`; each additional child and the BU LOV now has an object reference. The initial inventory identified seven ordinary P8 create candidates before the user narrowed scope. Parent supplier actions and attachment LOB transport facets are not silently counted as child lifecycle records.

User scope correction before artifact construction: ignore every DFF, every attachment, payment behavior, third-party payment relationships and all other non-required create fields. The full inventory is retained as proof of consideration, but excluded rows have no workflow/test route. Enabled payloads use required business fields only. P8 is reduced to business classifications, contact-address associations, contact data access, contact roles, merge history, products/services plus its view, site assignments and spend-authorization history.

Supported Ask Oracle binding is an Agentic App whose `queryAgent` references this workflow and invokes a dedicated `Query` route through `$context.$app.$OraMessageHint`. P1 must create and validate that wrapper, keep any DRAFT development flag explicit, and prove the actual signed-in Ask Oracle entry; debug chat remains insufficient. No runtime artifact, server definition or business data was written in P0.

## P1 implementation and learning — 2026-09-22

Created `XDX_SUPPLIER_LIFECYCLE_AGENT` and the `XDX_SUPPLIER_LIFECYCLE` DRAFT app. The workflow has distinct `Query`, `InitDisplay`, `InitActions` and `Summary` routes, a typed lifecycle object, deterministic revision and stale-approval handling and no business-data write nodes. The app contains one Supplier Lifecycle Advisor panel and uses the workflow as its query agent.

Historical evidence before the runtime-boundary audit: workflow DRAFT id `300000333950759`, version `86037127`, ETag 10; app DRAFT id `300000333951268`, version 1, ETag 1. The workflow suite passed 5/5; the workflow-debug Human continuation passed 73/73 assertions; the app panel `InitDisplay` test passed 1/1. These results remain valid for the paths they executed, but they no longer constitute P1 Agentic App Query acceptance.

Reusable tooling corrections were proven by red/green runtime evidence. Human Chat action discovery must infer approval and feedback capabilities from `approvalEnabledFlag` and `feedbackEnabledFlag` when `actionNames` is empty. Conversation retry assertions count every resume. App-triggered conversations must preserve the original `triggerType` and app parameters across Human resume calls; dropping the `Query` envelope caused repeatable FAI-50000 failures, and preserving it made the identical test pass. These changes belong to ATLAS conversation authoring/runtime guidance, not the supplier-specific workflow.

Independent recurrence audit invalidated the earlier P1 acceptance boundary. Workflow debug supports Human/wait continuation, while Agentic App Query does not; the only P1 app test exercised `InitDisplay`, so it never proved the required Query path. The earliest design blocker was Gate 0, and P1 should not have been accepted before a live target-app Query golden path. The app-bound workflow is being changed to contain no `HUMAN` or `WAIT` nodes, and the canonical playbook now makes structural non-suspension plus live target-app Query proof mandatory.

Workflow authoring learning: nested CODE output objects can render empty when bound directly into an LLM prompt in this runtime. Project the intended nested value to a scalar CODE output before the terminal LLM. A strict `RESPONSE_TEXT` data delimiter prevents a terminal LLM from interpreting cancellation text as a new instruction. Both patterns are covered by the current runtime suite.

## P2 implementation and runtime learning — 2026-09-22

P2 added read-only supplier name/number search and selected-supplier address, site and contact routes. Local contracts, structural validation, canonical workflow 7/7 and app 1/1 file-backed acceptance pass. No live POST is authorized or present.

Explicit user correction: testing is always in the context of AI Agent Studio, never another chatbot. The first signed-in attempt used the global Fusion Ask chat and was rejected as wrong-context evidence after it routed the supplier prompt to unrelated skills. The canonical playbook now requires recorded target-artifact Studio provenance before sending a prompt. The correct surface is the DRAFT `XDX Supplier Lifecycle` application opened from AI Agent Studio Applications through **Run app preview**.

Correct-context runtime evidence: the DRAFT app preview rendered the expected Supplier Lifecycle Orientation and returned Office Depot 1264 in the required four-column supplier result for `Find supplier Office Depot`. The next selected-supplier address prompt failed in the same preview with `AI Apps does not support workflows with wait nodes or human in the loop`. This is a target-runtime architecture failure: the app preview does not preserve the workflow design's Human/wait-node continuation. It blocks P2 signed-in multi-turn acceptance and must be repaired in the AI Agent Studio architecture before P3. The successful first query remains valid read-only evidence; the failed continuation is not replaced by generic chatbot or debug-chat proof.

After the first wait-free repair, the self-contained prompt `Show addresses for supplier number 1264` reached `LIST_SUPPLIER_ADDRESSES` and failed with HTTP 404. Independent audit confirmed the preparer had resolved `SupplierId`, but every child BO input still read the earlier reducer state where `parentId` was blank and the filter still described the supplier lookup. Earlier local tests injected a mocked child response directly into the normalizer, and the canonical suite asserted that all child BO nodes did not execute. The repair now routes both selected-state and self-contained child reads through the preparer and binds all address, site and contact inputs to that prepared state. The focused contract evaluates the routing and all downstream input expressions. Configured child-node execution and correct-context app-preview acceptance remain required before P2 can pass.

Correct-context targeted runtime after DRAFT v86044553 proved all three repaired child BO routes execute without the Human/wait error or 404. Address returned 10 rows under Address, City, State and Country; contact returned 10 rows under Contact, Job title, Email and Status. Site returned grounded rows but its first rendering omitted headings and exposed the terminal `RESPONSE_TEXT` label, so it was not accepted. DRAFT v86044912 replaced the ambiguous label with explicit copy boundaries and changed row markers from Markdown-list syntax. The one affected site rerun then returned 10 rows under Site, Procurement BU, Address and Status with no boundary leakage. P2 still requires refreshed configured child-node execution tests and cumulative acceptance before it can close.

## P2 configured-coverage closure — 2026-09-22

The focused ATLAS plan reached zero create/update actions and `finalSummaryAllowed: true`. Eight predicted branch combinations were deferred after two valid-route observations because they combine mutually exclusive upstream actions: standalone child prompts have no selected supplier, self-contained child prompts use `RESOLVE_CHILD_NUMBER`, and ordinary name search returns `NORMALIZE`. These deferred combinations were not executed or treated as passing runtime branches.

Executable configured coverage was retained in three custom observed-path tests. Each resolves supplier number 1264, executes the matching address, site or contact BO with the prepared `SupplierId`, and renders exactly four business columns. The canonical workflow suite passes 10/10, the app suite passes 1/1 and no judge remains. Together with the target AI Agent Studio DRAFT preview evidence, this closes P1/P2 for the current graph.

The child-resource scope is now presented for user review in `child-resource-slice-review.md`. It contains 12 independent slices. Each write slice remains gated by exact required-field research and live-POST approval; excluded DFF, attachment, payment and third-party relationship resources remain outside the implementation routes.
