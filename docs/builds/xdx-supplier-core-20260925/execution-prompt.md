# XDX Supplier Core — next execution prompt

Version1.1.0,2026-09-25. Model: [object-lifecycle1.1.0](../../build-models/object-lifecycle/v1.1.0/plan-template.md). Status: **prepared; not launched**. This file is not runtime or write authorization.

```text
Read docs/handoffs/ACTIVE_HANDOFF.md first, then execute
docs/builds/xdx-supplier-core-20260925/plan.md only when the user launches this build.

BUILD_ID=xdx-supplier-core-20260925
MODE=new-scoped-rebuild
BASE_REPO=C:/Users/dasu/Documents/GitHub/fusion-ai-studio-1
BASE_BRANCH=TestingWFBuildTools
BUILD_BRANCH=codex/xdx-supplier-core-20260925
BUILD_WORKTREE=C:/Users/dasu/Documents/GitHub/fusion-ai-studio-1/.worktrees/xdx-supplier-core-20260925
MODEL_VERSION=1.1.0
STUDIO_URL=https://fa-eqih-dev21-saasfademo1.ds-fa.oraclepdemos.com/hcmUI/redwoodAI

Deliver native DRAFT query and create for suppliers, addresses, sites and contacts
ONLY. BU GET is solely a site-field lookup. No other sub-object, business update,
business delete, account provisioning, batch create, email, PDF or custom UI.

Start UTC recording and acknowledge it at the first execution action. Read the
canonical playbook, registry, all five selected references and startup contract.
Resolve a committed base revision containing59fd032, this tracked clean plan,
model1.1.0 and the completed restart-preparation/purge receipt. Record the exact
launch SHA and hashes. Create the new worktree from that commit, never from dirty
governance. Fail on a branch/path collision; do not resume the retired build.
Verify the new checkout/branch; read its handoff/governance and pass Startup.

Use only the new checkout's active env.properties without exposing secrets.
Initialize fresh execution receipt, intake, timing, checkpoint and operation ledger;
all requirements and test results begin OPEN. Verify environment/authentication and
exact-code availability before saving new dated artifacts. Preserve all existing
BOs, accepted Fusion records and historical evidence. No purge is part of this run.

Record launch authority separately from this plan. Proposed live-write limit is
two accepted creates per resource: one direct-BO fixture chain and one native-app
fixture chain, eight new records maximum overall. This limit is authorized only
if the user's launch explicitly grants both chains. Native writes also require
exact-payload/revision approval in the app. If both chains are not authorized,
stop before dependent POST testing and resolve the evidence/authority conflict;
never spend a one-create budget twice. No business writes during preparation.

Use scoped sub-agents where independent work exists: contracts, one BO owner,
workflow logic and independent QA. One writer owns each shared artifact; a single
BO owner integrates all four resource contracts into the dated BO. Coordinator
owns the mutation queue, operation reservations, shared browser and final review.
Serialize tenant mutations and parent/fixture chains; retain operation handles.

For each resource dependency: freeze its contract, validate its actual BO template
and types locally, execute the actual saved BO GET functions independently, then
its authorized direct POST and independent persisted GET. Hand over versioned BO
readiness evidence for independent review before dependent workflow integration.
Raw REST calls and injected fixture responses cannot substitute for saved BO proof.
Independent local workflow logic may proceed while the BO readiness gate is open.

Prove the first actual DRAFT app Query and one complete supplier transaction
(prepare, field edit, review, exact approval, POST, independent GET, native result)
before cloning that pattern to children. Keep ordinary query acceptance separate
from create-confirmation GET. Resolve displayed SupplierNumber to verified
SupplierId; test the exact visible-number prompts in one app conversation.

Run actual serialized-code/template/binding checks before remote execution. Run
the Query preflight before applicable save/sync/record/runtime actions and after
graph, BO, producer or binding changes. Use corrected terminal-owner inference;
resolve stale/conflicting terminal evidence without dropping semantic judges.

For incremental changes use the affected-test planner with an explicit committed
baseline, union its selection with every required ATLAS and changed-contract test,
and select all on uncertainty. Do not rewrite bindings or hashes to skip work.
Run one final full approved configured suite after authoring is current. Account
for every outside-scope ATLAS suggestion without expanding the four-resource scope.

Use the timing wrapper for supported shell-free commands and retain activity,
dependency, overlap and rerun reasons. Maintain manual/browser intervals separately.
Observe the identified Studio tab at least every five minutes while acceptance is
pending; schedule four-minute reminders only if supported. Record unavailable
controls and gaps; no CLI token or written deadline proves browser keep-alive.
At70–75%context checkpoint/revalidate per playbook; remain terse in user updates.

At Gate9 after correctness, record current runtime performance and explicit model
sweep disposition. Run a model sweep only when launch authority requires it or a
specific approved performance repair needs it; otherwise defer to a named later
performance gate. Do not silently omit the decision or claim unmeasured savings.

Never replay an accepted POST. Reconcile uncertain writes read-only before any
retry. Preserve historical accepted IDs. Complete all eight requirement rows,
required configured judges, native rendering and independent persisted evidence.
Finish lessons, dependency cleanup, timing and current-task Closeout; commit scoped
work locally. Report verified DRAFT acceptance and limitations. No publish or push
for this new runtime build unless the user's launch explicitly changes that boundary.
```
