# Restart preparation intake

Task: xdx-supplier-core-restart-preparation-20260925. User explicitly authorized launch/template updates and local/server build purge, then a restart prompt. Existing Git/base synchronization authority persists; no remote branch deletion, publication or business-record deletion.

Design: issue object-lifecycle model1.1.0 with BO-first proof/write-budget/delegation gates, affected-test planner, command timing, exact-prompt/identifier acceptance and final packaging; keep canonical playbook sole lifecycle owner. Prepare next build xdx-supplier-core-20260925 with unique branch/worktree/artifact codes. Retire old launch pointers without erasing evidence. Purge only verified build-owned app/workflow and local implementation paths; evaluate own BO separately, preserving every shared/unknown-consumer provider and accepted Fusion records. Inventory before deletion, consumers before providers, exact readback. Preserve integrated Git history plus ignored/untracked evidence outside removed worktree. New build starts only when user invokes its prompt.

Objects: lifecycle deletion of Agent Studio app/workflow; no supplier API operation or schema modification. Four-resource query/create scope and read-only BU dependency remain locked in next plan; operation references remain unchanged.

Acceptance: template/prompt consistency, package tests, exact current server identities and independent absence proof, local path/ref absence with confined deletion, retained provider evidence, Startup/Closeout, scoped commits and exact remote SHA. Unknown consumers mean retain, never delete.

Outcome: model1.1.0 and fresh launch documents applied; exact artifact purge verified. Existing canonical lifecycle/object rules require no additional duplicate rule. Validation and retained-provider disposition: purge-receipt.md. No open implementation TODO; future launch gates are deliberately OPEN.
