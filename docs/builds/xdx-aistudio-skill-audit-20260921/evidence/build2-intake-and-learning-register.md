# Build Intake and Learning Register

Task: xdx-supplier-lifecycle-plan-20260920. Scope: phased plan, executable build/test prompt, server BO reuse discovery, isolated new worktree bootstrap. No application implementation or live supplier creation in this task.

Authority: current user request. Ask Oracle, agent lifecycle, four-column displays, source-based approved inheritance, exact draft approval, generated ID classification before each write slice. Detailed field research is deferred to execution as requested.

Selected references: Procurement suppliers, supplier-addresses, supplier-sites, supplier-contacts; Oracle26C/API11.13.18.05. Additional children require inventory and individual contracts in Phase0.

Lessons: prove actual conversational continuity first; keep state and approval at root; independent persisted GET is required but does not substitute for browser acceptance. Canonical playbook and object registry remain owners. No new evidence-backed lesson to promote during planning.

Environment: active root env.properties only. Reuse server XDX supplier BO definitions before creating dependencies; discover/read only now. No GitHub push or application publication. Worktree owner: current Codex task under explicit user instruction; cleanup after accepted integration or explicit abandonment, preserving evidence first.

## Planning findings
- Supported catalog found both XDX supplier BOs; 12 supplier functions and one BU lookup. Recommendation endpoint HTTP404 is bypassed through documented catalog, not hidden APIs.
- Sandboxed host credential access failed; supported unsandboxed retry authenticated successfully. No credentials recorded.
- BO metadata/fetch scope only; detailed current field research and supplier data operations deferred.
- Source snapshots retained for version/compatibility comparison; no runtime artifact adopted or modified.
- Existing source reference has eight GET functions on server, including validated filter, not just historical seven. Server evidence owns the reuse inventory.

## Execution intake
The plan owns outcomes, routes, state, phases, source scope and measurable correctness. P0 must fill authenticated execution identity, exact Ask Oracle binding, source field contracts/children, model placement, approved test identities and live-write counts before dependent artifacts or operations. These are execution gates, not completed planning evidence.

## Current execution task

Session/task: 01a0c19a-41a6-74f1-a699-31f0d2fda6db. Checkpoint: 2026-09-21T01:38:12.0874100Z. Execute approved P0-P9 plan in prepared worktree, preserving every runtime and Closeout gate. Earlier planning sections remain historical.
TaskStartSnapshot: root matches handoff; branch codex/xdx-supplier-lifecycle-agent; HEAD 44c0d23d3222c6ea8e6a5ca43d4d29f973022197; initial staged/unstaged/untracked lists empty; no active Git operation; no upstream configured. Two worktrees observed: base and this prepared build. Initial Git status emitted an inaccessible user-ignore warning; no task paths were reported.
Read set: entire canonical playbook, registry, all four core object references (26C / API 11.13.18.05), startup contract, plan/prompt and build records. Evidence from earlier runs is historical, not current runtime acceptance.
Intent/authority lock: current user explicitly executes plan. Local implementation/fixtures, needed read-only discovery and compatible BO modification are authorized. No live-create allowance, account provisioning, workflow CLI publication, GitHub push or destructive external cleanup. Exact publication decision deferred until concrete deployment target is reviewable. Active worktree env.properties is sole environment authority.
Readiness: P0 open; new startup receipt required before substantive work. No package directory observed; legacy src layout. Detailed payload research remains at each write slice. Current code change necessity and typed root owner follow approved plan. Strict TDD off/skipped; BUILD -> DISCRETE TEST -> CUMULATIVE TEST remains required. Inline execution, no delegation.

## P0 evidence checkpoint 2026-09-21T01:52:16.187Z

- Authenticated CASEY.BROWN from active configuration; current BO ETags 2 and 1 match planning snapshots exactly. Adopted server-normalized BOs locally; both validate with zero errors. No remote definition or supplier data write occurred.
- Oracle 26C specification: 61 supplier paths, 22 child collections, 15 collection POST operations. User excluded payment/access writes; 14 child create operations remain in business scope, all still disabled. Twenty new operation references include the BU lookup. Field research remains at its named slice.
- Local consumers: none found under src. Remote catalog returned 25 rows without paging metadata; no complete consumer claim. Server remains unchanged.
- Ask Oracle entry: current app prompt references support a top-level panel using a DRAFT workflow during development. Proposed minimal wrapper adds InitDisplay/Query stage separation and app tests. Exact wrapper contract must be frozen before artifact authoring; no replacement UI or abandoned code import.
- Corrections: initial receipt used uppercase hashes; verifier requires lowercase digest strings. Fixed and Startup passed before substantive discovery. Public spec retrieval failed in sandbox (PowerShell TLS; Node timeout); one unsandboxed HTTPS retrieval passed with normal TLS. Python unavailable; used existing Node/PowerShell. Initial path filter also matched nested suppliers under other API roots; anchored it to /suppliers and separately excluded enclosure paths from collection counts before recording final inventory.
- Gate reconciliation: registry/reference additions changed reviewed hashes. Reconcile expanded receipt now; the local BO adoption occurred before this hash refresh, so do not describe the initial receipt as continuously current through that documentation delta. No remote mutation followed.
- Learning disposition: resource findings routed to object references. No new universal lifecycle rule; existing TLS diagnosis, capture-once, intended-field and session-drift rules already cover observed behavior.
- Source-check necessity: verify-p0.cjs compares exact path inventory, parent keys, operation IDs, schema dependency closure, requirement/test mappings and adopted BO identity. It grants no runtime acceptance.

## P0 entry continuation task

Session/task: `01a0c19a-41a6-74f1-a699-31f0d2fda6db-p0-entry-20260921`. Started with committed checkpoint `6a63425c2fa505ba4fecdbe45ce7ca36660a7b8b`; worktree clean, correct branch, no active Git operation. Prior turn is classified as progress because it committed the exact child inventory, canonical BO adoption, source checks and evidence that determine this turn's next action.

Active slice: finish the supported Ask Oracle entry contract, then build P1 no-write conversation state. Intent, scope and acceptance remain unchanged. Payment relationships and contact roles/data access are read-only. No live POST, publication, push, account provisioning or external cleanup authority. Legacy `src` layout is retained; no new worktree or app package.

Change necessity: actual Ask Oracle runtime acceptance cannot be proven by a standalone workflow or debug chat. A minimum app-backed workflow plus app panel is necessary if current CLI/app contracts confirm that Ask Oracle reaches the panel agent's `Query` path. The wrapper owns only entry/stage routing and display; the root workflow remains the single conversation-state owner. Do not add actions, communications, summaries, extra panels or specialist workflows in P1.

Complexity budget: one workflow and one minimum app wrapper; typed state, deterministic reducer, root HUMAN and stage routing are distinct responsibilities inside the approved root workflow. Start with P1-only routes and zero POST nodes. If current CLI cannot author or test this topology without undocumented fields, stop before inventing schema.

Required evidence: current Startup receipt; exact local CLI/app/workflow contracts; local validation; ATLAS sync actions; configured-mode P1 suite; actual Ask Oracle entry with multi-turn continuity, correction, cancellation and stale-approval zero-write proof. Drift decision: continue; checkpoint, scope, BO identity, read-only payment/access boundary and test order agree.

## P0 entry contract freeze

- Artifact binding: app `XDX_SUPPLIER_LIFECYCLE_AGENT` (`src/apps/xdx_supplier_lifecycle_agent.app`) uses workflow `XDX_SUPPLIER_LIFECYCLE_AGENT` (`src/workflows/xdx_supplier_lifecycle_agent.wf`) as its only top-level panel agent. Remote DRAFT lookups found neither artifact code before local creation.
- App surface: one swimlane container titled `Supplier Lifecycle Advisor`; no extra panels, dedicated query agent, dynamic starter-query agent, priority actions, communications, templates or launch context. The configured panel agent owns normal Ask Oracle `Query`; `useDraftWorkflowWhileDeveloping=true` is required until UI publication is separately authorized.
- First load: `InitDisplay` reads one bounded page from `XDX_SUPPLIER_INFORMATION.ListSuppliers` and produces a grounded supplier overview with at most four business columns: Supplier number, Supplier name, Supplier type and Business relationship. It must disclose page bounds and must not expose SupplierId. This read-only GET is the confirmed external source.
- Query: the root workflow owns the typed `lifecycle` CONVERSATION object. A schema-constrained LLM interprets the current message; deterministic code validates/merges the allowed patch, advances revision and invalidates approval; root CHAT HUMAN nodes collect missing input or review the exact no-write intent draft. Cancellation, rejection and any change produce zero writes. P1 contains no POST node or attached create tool.
- Stage scope: implement distinct `InitDisplay` and `Query` routes only. The app agent is excluded from summary, actions and communications, so those stage behaviors are omitted. P2+ extends read routes; write routes remain gated by later object contracts and exact live-payload authority.
- Validation: local graph/state contract checks, workflow validation, focused ATLAS workflow/app synchronization, configured-mode no-write conversation suite, then actual Ask Oracle multi-turn correction, approval, cancellation and stale-approval evidence. Debug chat alone cannot accept P1.
- Checkpoint interpretation: the approved phased plan and execution instruction supplied and confirmed this exact outcome, data sources, minimal surface, no-action/no-communication scope, no launch context and validation route. No new structural choice was introduced for reconfirmation.

## P1 learning checkpoint

- AI Apps runtime validation rejects workflows containing HUMAN or WAIT nodes. Preserve conversational collection and approval through ordinary Ask Oracle turns and a typed conversation-state owner.
- When typed state owns continuity, configure the interpreting model to consume only the current message. Replaying chat history caused prior supplier values to appear as new patches during an approval turn; `chatHistoryEnabled=false` removed that ambiguity while deterministic state retained context.
- Use distinct outcome terminals for approval, rejection, cancellation and validation errors. This permits deterministic branch assertions without weakening the revision-bound approval gate.
- Runtime result: workflow DRAFT `300000333903401`, version `85893721`; app DRAFT `300000333903485`, version `1`. Workflow suite 5/5 and app suite 1/1 passed. Browser multi-turn acceptance covered correction, stale approval, exact approval, rejection and cancellation with zero writes.
- Learning disposition: the first two findings are reusable AI Studio lifecycle guidance and must be evaluated for promotion at Closeout. Object-operation references are unchanged because P1 performed no create and discovered no new REST field contract.

## P2 learning checkpoint

- Query terminals must own visible conversational output. In the DRAFT app runtime, an `oraInfoDisplay` terminal completed the call but the chat showed only `Done`; rendering the bounded four-column result as Markdown made the source-backed result visible.
- Page-only turns must omit resource, match mode and term changes. Deterministic merge then preserves the selected supplier and active child resource; this fixed `Next page` resetting address queries to supplier queries.
- DRAFT workflow `300000333903401` version `85899843` and app `300000333903485` version `1` passed the P2 runtime gate. Workflow tests passed 15/15; app tests passed 1/1; four impossible fresh-turn branches are recorded as deferred with `finalSummaryAllowed=true`.
- Browser acceptance covered exact and ambiguous supplier results, explicit selection, address paging, site and contact scope, four-column displays and no-row behavior. No write route ran. Payment relationships and contact roles/data access remain read-only.
- Learning disposition: evaluate the visible terminal and page-only patch rules for canonical playbook promotion at Closeout. No object reference changes are required because P2 used existing GET contracts and found no new field behavior.

## P3 learning checkpoint

- DRAFT workflow `300000333903401` version `85902239` adds parent-scoped address/site/contact reference selection, writable allowlists, apply-all, user-value precedence and complete no-write review state. Generated own IDs are excluded; only source-resolved foreign references may populate site drafts. Contact identity, email, account, role and access data are never inherited.
- Local P1-P3 checks and workflow validation passed. Focused sync is current; cumulative workflow tests passed 17/17 and app tests passed 1/1. Three judge results recorded minor wording omissions at 4/5: reference selection omitted the writes-disabled sentence, supplier disambiguation omitted the exact-number instruction, and final review omitted cancellation from its options.
- The create-address interpreter initially lost the target supplier query. Explicit CREATE address/site/contact parsing for labeled supplier number or exact supplier name restored the reference-selection route.
- Browser acceptance passed in the DRAFT app. The runtime resolved supplier 1252, required exact selection of `Lee US1`, exposed eligible defaults only, preserved user-value precedence, advanced revision 4 to 5 after a City correction and recorded exact revision 5 approval with no create attempt.
- No POST node exists or executed. Payment relationships and contact roles/data access remain read-only.
- Learning disposition: the CREATE child parent-resolution prompt rule is app-specific until later phases confirm reuse; no canonical or object-reference change yet.

## P4 intake checkpoint

- Active slice: supplier field research, exact request builder, revision-bound approval gate, guarded `CreateSupplier`, response ledger and independent supplier-detail GET.
- Selected reference: `docs/lessons/objects/oracle-fusion-procurement-suppliers.md`, Oracle Procurement 26C / REST 11.13.18.05. Reuse retained first-success GET and prior authorized POST evidence; do not refresh them for documentation.
- Required payload classification: every candidate must be writable business data, generated own ID, resolved reference, unique/non-copyable, read-only/audit or unresolved. Unknown classification blocks submission.
- Authority: complete local/fixture and DRAFT no-write work. No live POST until the exact active environment, one-create count, controlled unique identity and normalized payload are presented for scoped approval. Payment and access children remain read-only.
