# To-do list

- [ ] Avoid missteps like this: POST preparation defaulted to request-example mode. Make the intended example purpose explicit before preparation and execution; for an authorized live POST/read-back, use `examplePurpose: "response"`. Review guidance and add a focused prevention check without weakening payload validation or write authorization.
